//[?] &&(AND) 연산자: 둘 다 참일 때에만 참
using System;

class AndOperator
{
    
    {
        //[A] 둘 다 참일때에만 참
        console.log(true && true); // True
        //[B] 하나라도 거짓이면 거짓
        console.log(true && false); // False
    }
}
