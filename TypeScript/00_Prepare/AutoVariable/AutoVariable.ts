module AutoVariable {
    // 변수(Variable) 선언
    let num = 1234;
    let decimal = 12.34; 
    let c = 'A';
    let s = "Hello";
    let b = true; 

    // 숫자 구분자(Numeric Separator)
    //let numericSeparator = 1_000_000;
    //console.log(numericSeparator); // 1000000

    // 진법 표현
    let binary = 0b1111; // 15
    let hex = 0xFF; // 255
    let octal = 0o77; // 63

    console.log(binary);
    console.log(hex);
    console.log(octal);
}
