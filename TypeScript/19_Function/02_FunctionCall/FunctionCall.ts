//[?] 함수(Function): 반복하여 사용하도록 하나의 이름으로 만들어 놓은 코드의 집합

// 함수 정의 및 호출
namespace FunctionCall {
    //[1] 함수 만들기(정의, 선언)
    function hi() {
        console.log("안녕하세요.");
    }

    //[2] 함수 사용하기(호출): 여러 번 호출
    hi(); hi(); hi();
}
