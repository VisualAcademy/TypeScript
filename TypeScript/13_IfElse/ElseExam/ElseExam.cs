using System;

class ElseExam
{
    
    {
        // 콘솔로부터 문자 하나 입력받기
        char c = Convert.ToChar(Console.ReadLine());

        if (c == 'a')
        {
            console.log("a입니다."); // 입력한 문자가 'a'이면
        }
        else
        {
            console.log("a가 아닙니다.");
        }
    }
}
