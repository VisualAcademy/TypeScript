using System;

namespace If
{
    static void Main()
    {
        let score = 60;

        // score가 60 이상이면 "합격" 출력
        if (score >= 60)
        {
            console.log("합격");
        }
    }
}
