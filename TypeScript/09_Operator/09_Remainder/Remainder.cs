//[?] 나머지 연산자(Remainder Operator): 몫이 아닌 나머지 값 구하기
using System;

class Remainder
{
    
    {
        let first = 5;
        let second = 3;
        let result = 0;
        result = first % second; 
        console.log(result); // 몫: 1, 나머지: {2}
    }
}
